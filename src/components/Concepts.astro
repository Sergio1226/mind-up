---
const conceptos = [
  {
    title: 'Crisis',
    description: 'Estado temporal de trastorno y desorganización, caracterizado por la incapacidad del individuo para manejar situaciones particulares usando métodos habituales.',
    color: 'purple',
    icon: 'target'
  },
  {
    title: 'Discapacidad',
    description: 'Condición que resulta de la interacción entre las personas con deficiencias y las barreras debidas a la actitud y al entorno que evitan su participación plena.',
    color: 'pink',
    icon: 'users'
  },
  {
    title: 'Intervención',
    description: 'Conjunto de técnicas y estrategias terapéuticas aplicadas de manera sistemática para ayudar a las personas a superar dificultades y alcanzar el bienestar.',
    color: 'blue',
    icon: 'heart'
  }
];
---

<section id="conceptos" class="py-20 px-6 bg-white/50 backdrop-blur-sm">
  <div class="container mx-auto">
    <h2 class="text-4xl md:text-5xl font-bold text-center mb-4 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
      Conceptos Básicos
    </h2>
    <p class="text-center text-gray-600 mb-16 text-lg">Fundamentos esenciales de la psicología</p>

    <div class="grid md:grid-cols-3 gap-8">
      {conceptos.map((concept) => (
        <div class="group relative">
          <div class={`absolute inset-0 bg-gradient-to-r from-${concept.color}-400 to-${concept.color}-600 rounded-3xl blur-xl opacity-20 group-hover:opacity-40 transition-opacity duration-500`}></div>
          <div class="relative bg-white rounded-3xl p-8 shadow-lg hover:shadow-2xl transition-all duration-500 transform hover:-translate-y-2">
            <div class={`bg-gradient-to-r from-${concept.color}-500 to-${concept.color}-600 w-16 h-16 rounded-2xl flex items-center justify-center mb-6 transform group-hover:rotate-12 transition-transform duration-500`}>
              {concept.icon === 'target' && (
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-white">
                  <circle cx="12" cy="12" r="10"/>
                  <circle cx="12" cy="12" r="6"/>
                  <circle cx="12" cy="12" r="2"/>
                </svg>
              )}
              {concept.icon === 'users' && (
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-white">
                  <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                  <circle cx="9" cy="7" r="4"/>
                  <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
                  <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                </svg>
              )}
              {concept.icon === 'heart' && (
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-white">
                  <path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/>
                </svg>
              )}
            </div>
            <h3 class="text-2xl font-bold mb-4 text-gray-800">{concept.title}</h3>
            <p class="text-gray-600 leading-relaxed">{concept.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>